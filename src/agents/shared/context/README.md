# Context

Shared context assembly boundary.

Future implementation should build prioritized context blocks from current teacher input, student profile, recent learning records, wrong questions, teacher preferences, class context, and system rules.
