# Run

Shared Agent run lifecycle boundary.

This module defines status handling, generic run requests, runner wrappers, and
privacy-safe logging hooks for all future Agents. It must not contain specific
Parent Feedback Agent workflow logic.
