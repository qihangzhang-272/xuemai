# Codex Task Plan

## Current Phase

Phase 20: Original Xuemai workbench overview optimization.

## Current Focus

- Done: upgraded the original Xuemai first-level “工作台” into a complete independent overview instead of embedding the student/class chat list.
- Done: preserved the original green/white visual language and narrow first-level rail while separating “聊天” and “工作台” into two clear page modes.
- Done: added actionable metrics, a searchable/filterable work queue, item preview, direct chat positioning, today schedule, multi-select, and three batch-action previews.
- Done: enforced product safety boundaries in the interaction: batch generation creates separate drafts, parent feedback still needs per-student review, and long-term archives cannot be batch-confirmed.
- Done: completed desktop and 390px mobile browser QA plus lint, typecheck, 474 tests, and production build validation.
- Next: validate the queue ordering and batch-action vocabulary with real individual teachers and institution teachers before expanding management views.

## Previous Focus — PRD V4.0 Baseline

- Done: replaced the outdated PRD v3 with a single implementation-ready V4.0 product baseline dated 2026-08-06.
- Done: reconciled the chat-first Skill workflow, learning-material evidence boundary, feedback/archive state priority, analysis detail structure, monthly confirmed-source pool, privacy, Auth/RLS, and acceptance rules.
- Done: fixed the current product form as PC Web P0 plus responsive mobile Web P1; native apps, student/parent products, task plans, automatic messaging, and complex multi-Agent architecture remain outside this version.
- Done: documented the authority order `AGENTS.md` > `docs/PRD.md` > specialized technical specs > historical page/competitor documents.
- Next: run an implementation gap audit against `docs/PRD.md`, then schedule only the missing P0 items without expanding scope.

## Status

- Done: full v1.2 development guide archived in `docs/codex/`.
- Done: froze old `/dashboard` and continued the competitor restoration only inside `/workbench-v2`, `/workbench-v2/login`, `components/lessonledger/`, and `src/lessonledger/`.
- Done: restored the LessonLedger teacher workbench first screen with Qrane sample account, 12-item pending queue, today schedule, weekly overview, finance/account reminders, and account-card navigation.
- Done: restored parent portal front-end surfaces for recent courses, feedback, reports, billing, booking, notifications, and teacher binding, including Qrane-formatted feedback titles.
- Done: verified the current v2 front-end with typecheck, lint, production build, Chrome screenshots, HTTP 200 checks, and API mock baseline after restarting `127.0.0.1:3016`.
- Done: selected TypeScript/Next Route Handlers as the current v2 backend, backed by the existing file repository, instead of introducing Go before the single-process prototype is complete.
- Done: added LessonLedger backend contract tests for opened-slot booking validation, malformed time rejection, conflict adjustment and approval, finance balance synchronization, and feedback generation/publish persistence.
- Done: connected parent feedback cards to backend sent feedback drafts so the parent portal can survive refresh after a teacher publishes feedback.
- Done: promoted `createStudent` to a LessonLedger backend action, so new students persist through the API, create optional initial lesson-balance ledger rows, and can immediately generate family invites.
- Done: routed family invite generation, activation, and parent portal entry through the selected student id so newly invited parents land on the correct bound-student view.
- Done: promoted update feature requests to a backend action and persistent request queue so authorized users' needs survive refresh and appear in update/settings surfaces.
- Done: tightened parent-teacher communication so new threads require a bound student, linked feedback/course context, valid title/body, duplicate protection, and non-empty teacher replies; parent portal now only lists the selected student's own threads.
- Done: promoted class roll-call results to persistent attendance records, so each class lesson keeps per-student attendance, deduct-lesson flags, updated class member status, and no duplicate ledger rows on repeated saves.
- Done: hardened student score and weakness records with per-student validation, score/total parsing, rank checks, duplicate upsert behavior, latest-score sync, and empty states in the student profile.
- Done: removed hardcoded LessonLedger study-report copy so generated reports use the selected student's own scores, weaknesses, lessons, feedback highlights, sources, and dynamic suggestions.
- Done: tightened lesson-hour ledger and schedule moving behavior: manual deduction now rejects insufficient balances or wrong signs, and reset moves clear stale feedback drafts plus class attendance records.
- Done: added real family-invite expiry: generated invite links now carry a 48-hour expiry, activation validates parent phone/name, expired links become `已过期`, and invite UI shows the selected student plus expiry.
- Done: hardened parent booking conflict rules: duplicate same-student submissions are rejected, overlapping booking requests become `冲突`, teacher approval rechecks conflicts, and the parent/teacher booking UI now treats pending bookings as blockers alongside scheduled lessons.
- Done: hardened AI lesson-feedback contracts: content/state/homework now validate length, unsafe parent-facing wording is blocked on publish, attachment analysis no longer hardcodes a specific subject, and generated feedback copy follows the selected lesson inputs.
- Done: hardened schedule move behavior: moving lessons now checks active parent booking requests as well as existing lessons, and the move-confirm modal shows blocking course/booking conflicts before allowing confirmation.
- Done: polished the LessonLedger schedule/booking front-end so the schedule grid shows blue opened slots, yellow lessons, review/conflict booking markers, holiday opened-slot summaries, and no-side-effect booking modal interactions closer to the competitor demo.
- Done: completed the LessonLedger authorization/update demo loop: authorized update checks can discover a newer version, apply it without dropping course/ledger/family/report/booking data, and submit feature requests into the update queue.
- Done: tightened parent portal mobile layout: the parent navigation is isolated as a horizontal scroller, page/main/section containers now keep `min-w-0`, and booking cards wrap status chips inside the 390px viewport.
- Done: made the teacher dashboard overview dynamic: weekly lessons, completed lessons, lesson-hours, realized/pending income, account-risk counts, pending todo count, todo cards, and week bars now derive from current v2 state instead of fixed demo numbers.
- Done: hardened today lesson status handling: completing a lesson now rejects insufficient balances, duplicate completion does not duplicate ledger rows, and absence/cancellation clears stale feedback tasks and drafts.
- Done: documented the LessonLedger v2 backend choice in `docs/product/lessonledger-v2-backend.md`: use TypeScript/Next route handlers and the file-backed repository for the current single-process competitor clone rather than introducing Go prematurely.
- Done: added a service reload persistence test proving backend actions survive module reload through `.lessonledger-data/demo-store.json` instead of relying on React memory state.
- Done: extracted temporary video reference frames and current `/workbench-v2` screenshots for visual comparison, then captured the current completion matrix in `docs/product/lessonledger-v2-completion-audit.md`.
- Done: tightened the parent home teacher-binding card so it reads backend teacher profile fields instead of showing the placeholder “老师暂未填写简介”.
- Done: tightened LessonLedger teacher-side visual density: narrower sidebar, smaller buttons/pills/sections, denser dashboard rows, and shorter schedule/booking grid cells to move closer to the competitor's compact admin UI.
- Done: converted the teacher dashboard today schedule from card rows into a table-like list with fixed columns, and narrowed the schedule booking review rail to 300px with less redundant conflict actions.
- Done: compressed the teacher dashboard statistic blocks and schedule-grid lesson markers again, removing extra shadow/height so the first-pass `/workbench-v2` UI is closer to the competitor's dense admin table style.
- Done: tightened the parent portal home, latest-feedback detail, and feedback-linked communication modal; Chrome CDP verified the parent flow `home -> 查看全文 -> 发起沟通`, including a 390px mobile header fix.
- Done: compacted the teacher-side roll-call, family invite, and study-report generation modals; Chrome CDP verified attendance, invite, and report preview generation flows without runtime errors.
- Done: compacted the parent-side billing, study-report, booking grid, and booking-picker surfaces; Chrome/CDP verified the published report content and saved parent billing/report/booking screenshots.
- Done: tightened parent notifications and profile into denser portal views, then verified parent home/notifications/profile/booking/report at 390px with no horizontal overflow.
- Done: tightened teacher booking audit, booking conflict detail, license/update modal, and schedule drag-move confirmation; Chrome CDP verified conflict handling and drag confirmation states.
- Done: tightened `/workbench-v2/login`, the add-student modal, and the finance-ledger modal into denser LessonLedger-style operational surfaces; Chrome CDP verified desktop and 390px mobile no-overflow states.
- Done: tightened the score-record, weakness-record, and opened-slot modals into LessonLedger-style two-column operational panels; Chrome/CDP verified desktop and 390px mobile no-overflow states.
- Done: tightened the course create/edit modal and AI lesson-feedback modal into denser LessonLedger-style operation panels; Chrome/CDP verified desktop and 390px mobile states, including mobile feedback footer clipping fix.
- Done: tightened the dashboard today-course status row so real one-on-one lessons expose `已上课 / 学生缺席 / 取消`, completed lessons expose `去反馈`, and real class lessons expose `开始点名`; removed stale preset-row display mismatch.
- Done: added the PRD-required course status confirmation modal for `已上课 / 学生缺席 / 取消`, showing course, ledger, and feedback effects before writing status; Chrome/CDP verified desktop and 390px mobile modal states.
- Done: completed `/workbench-v2/login` error and authorization states: invalid credentials, missing/invalid teacher authorization, loading/success copy, demo-account quick fill, and 390px mobile no-overflow QA.
- Done: reinforced schedule-table drag-to-move confirmation with a pointer-based fallback, then Chrome/CDP verified the current build opens the reset/keep confirmation modal with original time, target time, conflict check, and confirm action.
- Done: tightened the teacher-side student management surface into a more competitor-like table workflow: student list, archive summary, score records, and weakness records now render as compact admin rows instead of large card blocks; Chrome/CDP verified desktop and 390px mobile no-overflow states.
- Done: restored the communication center closer to the competitor frame: teacher side now uses a three-column list/chat/feedback-assist layout with real status switching, while parent messages keep a readable mobile chat layout.
- Done: tightened student course, payment, and finance-timeline tabs into competitor-like ledger tables with status/action columns, replacing large metric-card panels; Chrome/CDP verified desktop and 390px mobile no-overflow states.
- Done: tightened the student family-account tab into a competitor-like account/invite table with current invite link panel and parent-portal sync log; Chrome/CDP verified desktop table, invite modal, 390px mobile table, and invite activation page.
- Done: tightened the study-report teacher and parent surfaces: teacher reports now use a report table plus source rail and detailed report body, while parent reports render a compact readable report; Chrome/CDP verified desktop, modal, and 390px mobile states.
- Done: tightened the booking conflict-exclusion loop: parent booking now shows auto-excluded conflict counts and quick no-conflict time suggestions, while teacher booking audit/detail surfaces show recommended adjustment slots; Chrome/CDP verified teacher desktop, conflict modal, parent desktop picker, and 390px parent booking states.
- Done: tightened the student archive first screen again: the large student table became a horizontal student switcher so the selected archive, horizontal tabs, and score ledger reach the first viewport sooner; Chrome/CDP verified desktop and 390px mobile states.
- Done: tightened the teacher dashboard first screen again: compacted statistic blocks, table rows, right-side todo rail, and week overview; added a 12-flow Chrome action smoke covering teacher/parent navigation, key modals, booking picker, feedback/attendance, finance/account/update actions, feedback communication, and 390px mobile health.
- Done: added a LessonLedger AI Provider backend boundary for feedback and study-report generation, with async route execution, deterministic fallback, unsafe-output filtering, and provider/fallback tests.
- Done: made the LessonLedger Chrome action smoke repeatable by resetting demo data before each run; re-ran the 12-flow front-end click smoke after build and confirmed zero failures and no duplicate-key console warnings.
- Done: root `AGENTS.md` upgraded to v1.2 project instructions.
- Done: lightweight Codex continuation docs created: `task-plan.md`, `dev-log.md`, `decision-log.md`.
- Done: created `docs/harness/` and `docs/product/` architecture entrypoints.
- Done: created `src/agents/` skeleton with registry, shared Harness Core, Parent Feedback Agent, Orchestrator, and planned Agent directories.
- Done: created non-destructive Supabase migration for Teaching Agent OS v1.2 data model.
- Done: separated AI runs, AI drafts, teacher-confirmed feedback, and confirmed learning records in schema.
- Done: created provider-agnostic shared model config and OpenAI-compatible client wrapper with DeepSeek defaults.
- Done: implemented Shared Harness Core skeleton for registry, run lifecycle, context assembly, tool registry, logging, evaluation, guardrails, and recovery.
- Done: implemented Parent Feedback Agent backend contract, context loading, prompt, guardrails, evaluator, one-pass revision, workflow, API route, and draft logging.
- Done: implemented teacher confirmation save flow through `POST /api/agents/parent-feedback/confirm`.
- Done: teacher-confirmed feedback writes `feedback_history`, confirmed `student_learning_records`, updates `agent_outputs`, and records `teacher_edit_events` when edited.
- Done: student detail page supports generate, edit, copy, and save for Parent Feedback Agent without auto-sending WeChat.
- Done: Phase 7 E2E test passed through page UI and Supabase read-only verification.
- Done: imported latest PRD v3 Harness direction into `docs/PRD.md`.
- Done: replaced root `AGENTS.md` with the shorter Workflow-first development guardrails while preserving concise logging rules.
- Done: added `docs/ENGINEERING_GUIDE.md` and `docs/AI_SKILL_SPEC.md` as implementation summaries.
- Done: added `src/skills/` registry, types, actions, and mock runner as the new MVP frontstage execution boundary.
- Done: current dashboard Skill capsules now come from Skill Registry, and mock SkillCards are generated through Skill Runner.
- Done: hardened Parent Feedback real-write API so `teacherId` is session-derived and confirm archive uses an idempotent RPC/migration path.
- Done: added a frontstage SkillCard Runner adapter so copy/send/archive actions pass through `transitionSkillRun` instead of ad hoc UI-only state checks.
- Done: routed non-state SkillCard actions such as add-to-monthly-material, generate-practice, save-note, and follow-up generation through traceable `SkillRunEvent` records.
- Done: exposed traceable SkillRun events in the right-side review panel as teacher-readable operation timeline rows.
- Done: E4 local persistence contract maps mock `ChatState` into `skill_runs`, `skill_cards`, `skill_card_events`, `skill_card_edits`, and timeline replay records.
- Done: E5 Supabase compatibility draft adds Skill Shell tables and a pure adapter plan without real database writes.
- Done: E6 adds a server-side dry-run Skill persistence endpoint that derives teacher identity from Auth and refuses real sync by default.
- Done: E7 adds a Skill Shell RLS migration draft scoped to authenticated teacher-owned rows without delete policies.
- Done: E8 adds a feature-flagged server-side Skill persistence write path that remains disabled by default.
- Done: E9 documents the dry-run and real-write manual QA gate for Skill Shell persistence.
- Done: E10 adds minimal route-boundary tests for Skill persistence auth, dry-run ownership, and disabled real writes.
- Done: E11 adds SkillCard persistence migration contract tests and verifies the feature-flagged write route against a fake Supabase client.
- Done: E12 adds a read-only Supabase readiness check SQL for Skill Shell tables, indexes, RLS, policies, and auth.uid inspection.
- Done: E13 adds a server-only boundary test to prevent UI/shared frontend imports of the service-role Supabase client.
- Done: E14 tightens environment-file ignore rules so `.env*` stays untracked while `.env.example` remains trackable.
- Done: E15 adds a pure Skill Shell readiness result interpreter so manual Supabase SQL output can be checked locally before enabling real writes.
- Done: G0 safety gate rechecked Parent Feedback Auth-derived teacherId, confirm idempotency, and added route-level class ownership coverage.
- Done: G1 adds all-subject `analyze_learning_evidence` report schema and mock runner output without real AI, Supabase, or PDF export.
- Done: G2 connects `analyze_learning_evidence` full reports to the right-side review panel while keeping chat SkillCards as compact summaries.
- Done: G3 adds teacher-selectable mock student-profile update suggestions from learning evidence reports; confirmed selections flow into local timeline/profile update projections only after archive.
- Done: G4 connects confirmed profile update projections to the student ContextPanel with a monthly material pool and filtered archive timeline.
- Done: K12 professional evaluation analysis contract adds material classification, per-question analysis, teacher professional report, parent feedback draft, monthly snapshot/comparison seed, and provider-replaceable model contract.
- Done: K12 analysis evaluation harness now separates synthetic smoke fixtures from human-labeled 99% claim datasets and fails empty or partially failed batches.
- Done: K12 evaluation file loader supports inline JSON datasets and relative `gold_path` / `analysis_path` datasets for model/provider regression checks.
- Done: `npm run eval:k12-material` runs the dataset gate from `XUEMAI_EVAL_DATASET`; synthetic fixtures require explicit `XUEMAI_EVAL_ALLOW_SMOKE=1`.
- Done: student `monthly_report` mock now uses a deterministic confirmed-snapshot aggregator; unconfirmed, wrong-student, or wrong-month learning material snapshots are filtered before monthly summary and month-over-month comparison.
- Done: monthly report source pool now also accepts confirmed learning records, confirmed feedback history, and teacher notes while keeping per-question trends tied to confirmed learning material snapshots.
- Done: learning material analysis model calls now use `createLearningMaterialAnalysisModel` as a provider-agnostic adapter, with DeepSeek kept as a compatibility alias.
- Done: `npm run generate:k12-analysis-output` can turn a `VisionEvidencePacket` file plus optional answer/rubric/profile JSON into a persisted `StudentLearningMaterialAnalysis` artifact for dataset `analysis_path`.
- Done: human gold labels now have a double-label/adjudication package validator, and dataset cases can reference `gold_label_package_path` directly.
- Done: validated `StudentLearningMaterialAnalysis` can now be assembled into user-facing `teacher_report`, `parent_feedback`, and `monthly_result` objects without exposing raw internal evidence refs in report copy.
- Done: public mainland K12 curriculum/evaluation references are captured as a compact code module for stage/subject recognition, report ability dimensions, and mismatch review warnings.
- Done: `npm run generate:k12-user-result` can now generate the final user-facing result artifact from either an existing analysis JSON or the full VisionEvidencePacket -> model -> result path.
- Done: frontstage learning-material review panels now prefer `StudentLearningMaterialUserFacingResult` and keep legacy `learning_evidence_report_v1` as a fallback for old mock cards.
- Done: deterministic K12 material pre-classification now runs from `VisionEvidencePacket` before text reasoning, and degraded analyses reuse the same material/subject/stage/grade/curriculum classifier.
- Done: deterministic per-question evidence readiness now gates model output, so missing answer basis, missing student answer, unstable segmentation, or low confidence cannot pass as definitive correctness.
- Done: per-question evidence readiness now accepts explicitly `question_id`-mapped external `answerKeys` / `rubrics` as answer basis while still blocking missing student answers or mismatched question IDs.
- Done: `npm run validate:k12-eval-assets` now validates replayable 99% evaluation asset manifests before dataset eval, including VisionEvidencePacket, gold package, side-input answer/rubric files, evidence refs, and per-question readiness alignment.
- Done: open-source OCR/Vision/Layout candidates were evaluated in `docs/product/k12-paper-analysis-skill-design.md`; no candidate replaces the K12 evidence gate, but PaddleOCR/PaddleX is the first provider candidate and Pix2Text/Surya/MinerU are comparison or specialty candidates.
- Done: `VisionEvidencePacket` now supports polygon geometry and optional pipeline trace metadata for OCR/Layout/Vision stage audit.
- Done: per-question evidence readiness now treats missing `crop_ref` as unstable segmentation, so bbox-only visual regions route to teacher review instead of definitive correctness.
- Done: per-question evidence readiness and question-segmentation QA now treat critical same-question evidence without `region_id` attribution as teacher-review-only, so OCR/Vision output cannot support hard judgement or definitive gold unless the evidence is tied to a question region.
- Done: evaluation asset manifest reports now include coverage for material states, material types, subjects, education stages, Vision providers, side-input answer/rubric cases, and human gold package count.
- Done: refreshed open-source OCR/Vision search with Marker, MonkeyOCR, olmOCR, and DeepSeek-OCR as additional candidates; still no open-source project replaces the K12 evidence gate or human gold dataset.
- Done: added a provider-neutral OCR/Vision adapter that normalizes external page, question, region, and evidence outputs into `VisionEvidencePacket` with pipeline trace, gates, provider/model metadata, and crop-ref readiness boundaries.
- Done: added `npm run generate:k12-vision-packet` so external OCR/Vision provider adapter input can be replayed into a validated `VisionEvidencePacket` artifact before analysis generation and dataset eval.
- Done: tightened 99% claim policy so passing metrics are not enough; claimable datasets must also cover sufficient hard-judgement questions, teacher-review questions, material types, subjects, primary/middle/high stages, and region/curriculum signals.
- Done: added a confirmed monthly snapshot copy helper so analysis Skill monthly candidates only enter the monthly report source pool after explicit teacher archive metadata is attached.
- Done: added `npm run generate:k12-monthly-report` so confirmed monthly snapshots, confirmed records, feedback, teacher notes, and previous-month sources can be replayed into a `student_monthly_report_v1` artifact.
- Done: extended `validate:k12-eval-assets` so replayable evaluation manifests can cross-check optional `analysis_path`, `result_path`, and `monthly_report_path` artifacts against the same case/student/material.
- Done: external VisionEvidencePacket trace now records selected OCR/Layout/Vision provider plus open-source candidate/benchmark list with license notes, so provider changes stay auditable before dataset reruns.
- Done: deterministic material pre-classification now recognizes broader mainland region/curriculum signals, including province-level regions, common city markers, national/new-Gaokao exam volumes, and common textbook versions.
- Done: K12 evaluation now scores material classification across material type, subject, stage, grade, and region/curriculum, and asset preflight rejects analysis artifacts whose grade or region/curriculum diverges from adjudicated gold labels.
- Done: deterministic material pre-classification now covers more common mainland city signals and routes conflicting region/curriculum-version hits to review warnings with lower confidence instead of a single hard guess.
- Done: deterministic material pre-classification now recognizes more real-world mainland material and textbook wording, including 课时作业、随堂练习、限时训练、诊断卷、小升初、青岛版、北京版、鲁科版、仁爱版、苏科版、沪粤版 and related local-version labels, while keeping false regional signals and conflicting curriculum hits on the teacher-review path.
- Done: deterministic material pre-classification now covers more real tutoring aliases such as 周末作业、寒暑假作业、校本作业、晨测、堂清、综合练习、期中/期末复习卷、专题卷 and related short-cycle quiz wording without bypassing student-trace evidence gates.
- Done: `npm run generate:k12-gold-label-template` now creates non-claimable human labeling draft packages from validated VisionEvidencePacket files, giving the human gold dataset workflow a repeatable starting point before anonymization, double labeling, and adjudication.
- Done: `npm run generate:k12-eval-assets-manifest` now creates and validates replayable evaluation asset manifests from case asset path lists, reducing manual JSON assembly before `validate:k12-eval-assets`.
- Done: `npm run generate:k12-eval-artifacts` now expands analysis-backed evaluation manifests with user-facing result artifacts and optional monthly report artifacts, then writes a validated updated manifest without calling the live reasoning model.
- Done: refreshed open-source annotation-tool options for the human gold dataset chain: Label Studio is the first labeling UI candidate, CVAT is the visual-region gold/QC candidate, and X-AnyLabeling remains an offline experiment option because of GPL-3.0 review risk.
- Done: `validate:k12-eval-assets` now reports `claimable99AssetReady` blockers, so a structurally valid manifest is not confused with a representative 99%-claim-ready asset set.
- Done: added `npm run generate:k12-gold-label-package` to convert normalized annotation imports from Label Studio/CVAT/manual labeling into validated gold packages aligned with the same VisionEvidencePacket.
- Done: added `npm run compare:k12-model-regression` so model/provider/prompt/schema changes compare candidate outputs against a baseline on the same gold dataset before acceptance.
- Done: added `npm run generate:k12-delivery-bundle` to package the teacher report, parent feedback, monthly comparison, feedback/archive status, safeguards, and source map into a single teacher-facing result artifact.
- Done: `npm run generate:k12-delivery-bundle` now accepts `XUEMAI_RESULT_INPUT`, `XUEMAI_ANALYSIS_INPUT`, or `XUEMAI_VISION_PACKET`, so teachers/product flows can generate the final delivery artifact without manually chaining intermediate files.
- Done: refreshed related open-source project scan with OCR/Layout/Formula candidates plus Chinese K12 benchmark/reasoning references; the scan is now captured in the Skill reference and keeps public benchmarks separate from project-owned human gold datasets.
- Done: evaluation asset manifests now support `delivery_bundle_path`, and `validate:k12-eval-assets` cross-checks the teacher delivery bundle against the same VisionEvidencePacket, analysis, result, monthly report, source map, and user-visible status boundaries.
- Done: `npm run generate:k12-eval-artifacts` now expands analysis-backed manifests with final teacher delivery bundle artifacts by default, so the product-facing handoff artifact is replayable before any 99% claim readiness review.
- Done: added `npm run generate:k12-question-segmentation-review` to produce redacted question-segmentation QA artifacts from VisionEvidencePacket files before gold labeling or 99% asset preflight.
- Done: evaluation asset manifests now support `question_segmentation_review_path`, and `validate:k12-eval-assets` requires definitive gold questions to have matching `pass` segmentation review entries before claim readiness.
- Done: `npm run generate:k12-eval-artifacts` now expands manifests with question-segmentation review artifacts by default, alongside user-facing result, delivery bundle, and optional monthly report artifacts.
- Done: gold label template generation can now read `XUEMAI_QUESTION_SEGMENTATION_REVIEW` and copy each question's segmentation status/issues into reviewer notes before human labeling.
- Done: annotation import to final gold package conversion can now read `XUEMAI_QUESTION_SEGMENTATION_REVIEW` and reject definitive gold judgements for questions whose segmentation review is not `pass`.
- Done: teacher delivery bundles now have `validateStudentLearningMaterialDeliveryBundle`, which checks professional report sections, per-question rows, safe parent feedback, month-over-month comparison wording, feedback/archive display status, safeguards, and source map before writing or asset acceptance.
- Done: user-facing result artifacts now have `validateStudentLearningMaterialUserFacingResult`, which checks professional report sections, per-question rows, safe parent feedback, monthly comparison wording, source labels, and hidden internal evidence refs before writing or asset acceptance.
- Done: `validateVisionEvidencePacket` now rejects duplicate or orphan page/question/region/evidence references, gate evidenceRefs, and pipeline trace output_refs before text reasoning, and mock fixtures now use stable evidence_ref values in gates.
- Done: external OCR/Vision adapter inputs now pass `validateExternalVisionAdapterInput` before packet generation, blocking missing provider metadata, empty page/question arrays, duplicate IDs, cross-question evidence, orphan region refs, invalid confidence, helper-template placeholders in ID/ref/path fields, and invalid bbox/polygon geometry before a `VisionEvidencePacket` is written.
- Done: open-source project scan was rechecked on 2026-06-20; PaddleOCR/PaddleX remains the first real Provider candidate, while MinerU/Pix2Text/DocLayout-YOLO/LaTeX-OCR/EduNLP/EduData/Label Studio/CVAT stay as specialty, benchmark, or labeling-chain candidates rather than replacements for the Skill evidence gate.
- Done: `npm run generate:k12-gold-label-annotation-task` now creates a redacted Label Studio/CVAT/manual annotation task package from VisionEvidencePacket plus question-segmentation review, including crop refs, geometry, evidence refs, tool payload hints, and annotation import skeleton without raw OCR text.
- Done: evaluation asset manifests now support `annotation_task_path`; `validate:k12-eval-assets` cross-checks the redacted annotation task against VisionEvidencePacket, question-segmentation review, final gold package, and raw-text exclusion before claim readiness.
- Done: `npm run generate:k12-eval-artifacts` now expands manifests with redacted annotation task artifacts by default alongside question-segmentation review, user-facing result, delivery bundle, and optional monthly report artifacts.
- Done: student monthly report artifacts now carry `comparison_evidence`, and `validate:k12-eval-assets` rejects month-over-month trend claims without replayable previous-month source ids while adding monthly comparison evidence coverage to `claimable99AssetReady`.
- Done: open-source OCR/Layout/Formula scan was refreshed with UniMERNet, LaTeX-OCR/pix2tex, and docTR as specialty or baseline candidates; none replaces PaddleOCR/PaddleX first-provider trial, `VisionEvidencePacket`, or human gold validation.
- Done: added `npm run generate:k12-provider-trial-report` to create a redacted Provider trial report from VisionEvidencePacket, summarizing provider candidates, gates, question segmentation counts, per-question readiness, blockers, warnings, and next steps without raw OCR text.
- Done: evaluation asset manifests now support `provider_trial_report_path`; `validate:k12-eval-assets` cross-checks the Provider trial report against the same VisionEvidencePacket and optional question-segmentation review before claim readiness.
- Done: `npm run generate:k12-eval-artifacts` now expands manifests with Provider trial reports by default alongside question-segmentation review, annotation task, user-facing result, delivery bundle, and optional monthly report artifacts.
- Done: `validate:k12-eval-assets` now reports Provider trial readiness coverage and blocks `claimable99AssetReady` when any Provider trial report remains `blocked`.
- Done: open-source project scan was supplemented with GitHub API negative results for `试卷 OCR PaddleOCR`, `题目切分 OCR`, and `exam paper OCR question segmentation`; no vertical repo replaces the Skill evidence gate, so generic OCR/Layout/Formula projects remain Provider candidates only.
- Done: user-facing result and teacher delivery bundle validators now require exact `question_id` coverage and order for per-question rows, preventing a final teacher artifact from duplicating one question while omitting another.
- Done: professional teacher reports now include additional evaluation sections for learning-transmission path, difficulty-layer feedback, learning-strategy feedback, subject-ability feedback, key-mistake causes, performance space, and recurrence risk; result/delivery validators reject artifacts missing key professional sections.
- Done: model/provider/prompt/schema regression comparison now validates generated user-facing result and teacher delivery bundle artifacts for every baseline/candidate case, so candidate models cannot pass on internal metrics while breaking the final teacher/parent result.
- Done: model/provider/prompt/schema regression comparison now also validates generated teacher review packet artifacts, and the review packet validator rejects action enablement, summary counts, source labels, or audit source maps that no longer match the same delivery bundle.
- Done: redacted annotation task validation now rejects any task package that copies OCR/normalized/text content from the source VisionEvidencePacket into notes, checklists, payloads, or other editable fields, so human gold upstream artifacts remain raw-text-safe.
- Done: final gold package validation now also rejects notes that copy OCR/normalized/text content from the same VisionEvidencePacket, and annotation-import conversion treats this leak as a hard failure even when incomplete draft generation is allowed.
- Done: Provider trial reports and question-segmentation review artifacts now also reject copied OCR/normalized/text content from the same VisionEvidencePacket, and asset preflight propagates those leak failures without echoing the raw text.
- Done: annotation import conversion now rejects annotation import notes that copy OCR/normalized/text content from the same VisionEvidencePacket before writing final gold packages.
- Done: `validate:k12-eval-assets` now reports Vision source kind and mock/synthetic source coverage, and blocks `claimable99AssetReady` for `human_labeled` manifests that still contain mock or synthetic Vision sources.
- Done: `eval:k12-material` now requires dataset-level `asset_preflight.claimable99AssetReady=true` before returning `claimable99Correctness=yes`, so metric-only datasets cannot bypass asset manifest preflight.
- Done: the dataset file runner's top-level `ok` now follows `claimable99Correctness`, while `result.ok` remains the metric-only signal; metric-only pass datasets without asset preflight now fail the formal runner gate.
- Done: `npm run generate:k12-eval-dataset` now converts a preflighted evaluation asset manifest into an `eval:k12-material` dataset with `asset_preflight` metadata copied from the same manifest validation result.
- Done: `eval:k12-material` now requires claim-ready datasets to preserve per-case `asset_manifest_case_id` plus replayable final `gold_label_package_path` and `analysis_path`, so handwritten `asset_preflight` cannot make ad hoc `gold_path` datasets claimable.
- Done: `validate:k12-eval-assets` now reports Provider candidate trace, benchmark/open-source candidate, and license/deployment review note coverage, and blocks `claimable99AssetReady` when real OCR/Vision cases lack those provider evaluation records.
- Done: `vision-adapter` default Provider candidate trace now includes Docling, MonkeyOCR, olmOCR, and DeepSeek-OCR as benchmark-only document/OCR parser candidates with source URLs and license/deployment review notes.
- Done: `npm run compare:k12-vision-provider-regression` now compares same-material baseline/candidate Provider trial reports before downstream reasoning, blocking OCR/Vision regressions in question coverage, crop_ref coverage, segmentation status, evidence readiness, and confidence.
- Done: `npm run generate:k12-gold-label-review-report` now creates a source-text-safe double-label agreement/adjudication report from final gold packages, and `validate:k12-eval-assets` blocks claim readiness when the report is missing or not 99%-ready.
- Done: `npm run generate:k12-eval-case-package` now creates a privacy-safe real-sample case package scaffold with artifact path conventions, command sequence, 99% readiness checklist, and `evaluation-assets.cases.json` without copying raw student materials.
- Done: `npm run inspect:k12-eval-case-package` now inspects a real-sample case package, reports privacy-boundary violations, missing artifacts, the next sequential command, asset-preflight status, and 99% blockers while keeping `claimable99_from_case_package=false`.
- Done: case package readiness inspection now also reports grouped OCR/Vision, question-segmentation, human-gold, analysis/result, monthly-comparison, teacher-delivery, and asset-manifest chain status so annotation import or monthly evidence gaps are visible before asset preflight.
- Done: `npm run audit:k12-eval-claim` now reads a dataset and outputs structured 99% claim gaps plus next sample targets for case/question count, definitive/teacher-review question mix, material type, subject, primary/middle/high stage, region/curriculum, review protocol, and asset preflight coverage.
- Done: claim audit now splits asset-preflight blockers into artifact-focused next targets and shows them in the text report, so missing Provider trial reports, annotation imports, monthly comparison evidence, or teacher review packets can guide the next collection batch without being mistaken for correctness proof.
- Done: `runAnalyzeLearningEvidence` now blocks non-`valid_student_material` states before model calls, so teacher resources, insufficient student traces, low-quality materials, and ambiguous needs-review packets become teacher-review drafts instead of student ability analysis.
- Done: `npm run generate:k12-teacher-review-packet` now wraps a delivery bundle into a teacher-reviewable SkillCard/analysis-detail packet with action availability, feedback/archive status boundaries, hidden internal evidence refs, no auto-WeChat sending, and no auto-archive.
- Done: open-source OCR/Layout scan was refreshed against current primary repositories; no direct Chinese K12 analysis system was found, so PaddleOCR/MinerU/Pix2Text/RapidOCR/Surya/CnOCR/DocLayout/LayoutParser remain Provider candidates or baselines behind `VisionEvidencePacket`.
- Done: `npm run generate:k12-teacher-review-packet` can now start from an existing delivery bundle or generate the delivery bundle first from result/analysis/VisionEvidencePacket inputs, while preserving teacher review and no-auto-send/no-auto-archive boundaries.
- Done: final user-facing result, delivery bundle, and teacher review packet now carry source-labeled structured material classification fields, so UI can display material type, subject, stage, grade, and region/curriculum without parsing report markdown or exposing raw `evidenceRefs`.
- Done: final user-facing result, delivery bundle, and teacher review packet now carry structured previous-month evidence status/source counts, so monthly longitudinal comparison readiness is not inferred from Chinese copy text.
- Done: `validate:k12-eval-assets` now reports Provider candidate source URL coverage and blocks `claimable99AssetReady` when OCR/Vision candidate traces lack replayable `evidence_source_url` records for the selected provider, fallback, or benchmark/open-source candidates.
- Done: real-sample case packages now include an explicit `monthly_report_input_path` and `monthly_report` command step before delivery bundle generation, so current-vs-previous-month comparison evidence cannot be skipped in the 99% asset chain.
- Done: refreshed the open-source OCR/Layout/Formula scan with UniMERNet and added UniMERNet to the Vision adapter provider candidate trace as a formula-only benchmark candidate with source URL and license note.
- Done: teacher professional reports now include a required `证据充分性判定` section, and user-facing result plus delivery bundle validators reject final artifacts that omit the hard-judgement vs teacher-review boundary.
- Done: teacher review packets now also validate that the teacher-visible report preserves `## 证据充分性判定`, so SkillCard / analysis-detail handoff cannot drop the hard-judgement vs teacher-review boundary.
- Done: refreshed the open-source scan with Docling, K12Vista, and OCRBench / MultimodalOCR as document-conversion or benchmark references only; no direct Chinese K12 student-material analysis system was found.
- Done: question-segmentation review validation now recomputes the packet-derived segmentation review from the same VisionEvidencePacket and rejects tampered `pass` statuses, crop refs, issues, or definitive-readiness flags that do not match source evidence.
- Done: Provider trial report validation now recomputes the packet-derived report from the same VisionEvidencePacket and rejects tampered readiness, summary counts, blockers/warnings, Provider candidates, or per-question segmentation/readiness fields that do not match source evidence.
- Done: monthly report asset validation now requires previous-month comparison source counts to match replayable non-empty source ids, rejects duplicate/blank previous-month source ids, and rejects missing-status comparison evidence that still carries previous-month sources or marks a previous report as used.
- Done: evaluation asset manifests, case packages, and `generate:k12-eval-artifacts` now support `teacher_review_packet_path`; asset preflight validates each teacher review packet against the same delivery bundle and blocks 99% asset readiness when review packet coverage is missing.
- Done: evaluation asset manifests now support `annotation_import_path`; `validate:k12-eval-assets` converts the normalized annotation import against the same VisionEvidencePacket/question-segmentation review and blocks 99% asset readiness when it is missing or cannot reproduce the final gold package.
- Done: monthly report file input validation now rejects unconfirmed snapshots/sources, mismatched current/previous month entries, missing SkillRun/archive/source-material replay ids, and current or previous monthly sources without non-empty evidenceRefs before report generation or case-package readiness can continue.
- Done: evaluation asset manifests now support `monthly_report_input_path`; `validate:k12-eval-assets` validates the same teacher-confirmed monthly input artifact and blocks claim readiness when monthly input coverage is missing.
- Done: `audit:k12-eval-claim` now maps missing `monthlyReportInput` asset-preflight blockers to a dedicated `monthly_report_input` artifact focus instead of confusing them with missing monthly report output.
- Done: user-facing monthly comparison now requires replayable `previous_month_snapshot.evidenceRefs`; without them, teacher monthly notes and `monthly_result` show missing-evidence wording instead of explicit progress/regression trends.
- Done: question-segmentation review and redacted annotation task validation now require question order to match the same VisionEvidencePacket, including annotation import skeleton and Label Studio payload order, so upstream human-gold artifacts cannot be silently reordered before final gold generation.
- Done: `StudentLearningMaterialAnalysis` evidenceRefs are now validated against the same VisionEvidencePacket, allowed question-mapped side inputs, or monthly comparison previous-month refs; runner and asset preflight reject invented or cross-case refs.
- Done: explicit non-K12 scope signals now block before text reasoning and asset preflight; university, adult, vocational, postgraduate, non-K12 external exam, or professional qualification materials cannot be analyzed as K12 student ability cases.
- Done: material classification now treats explicit Hong Kong/Macau/Taiwan exam-system and IB/A-Level/IGCSE/AP/SAT materials as outside mainland K12 claim scope, and formal middle-school grade wording such as `初中一年级` maps to middle school rather than primary.
- Done: `StudentLearningMaterialAnalysis.question_analyses` now must exactly match the same VisionEvidencePacket question ids and order; runner and asset preflight reject missing, extra, duplicate, or reordered question rows, and degraded drafts keep every recognized question as a teacher-review row.
- Done: human gold package validation can now use `XUEMAI_VISION_PACKET` / `sourcePacket` to require packet identity, question coverage/order, and same-question evidence basis refs before a final gold package is accepted.
- Done: `generate:k12-gold-label-review-report` file generation is covered by regression tests for source-packet cross-question evidence-basis failures, and docs now recommend passing `XUEMAI_VISION_PACKET` for real gold review reports.
- Done: external OCR/Vision adapter input and VisionEvidencePacket validation now reject evidence whose `region_id` belongs to a different `question_id`, preventing cross-question visual-region attribution before text reasoning or asset preflight.
- Done: case package readiness inspection now validates an existing `vision_packet_path` VisionEvidencePacket artifact and returns `fix_artifact_validation` before downstream Provider trial, segmentation, gold labeling, analysis, monthly, delivery, or dataset commands when the packet is structurally invalid.
- Done: case package readiness inspection now also validates existing `question_segmentation_review_path` and `provider_trial_report_path` artifacts against the same VisionEvidencePacket, surfacing tampered segmentation pass statuses or Provider trial summary/readiness mismatches as `fix_artifact_validation` before human gold or analysis commands.
- Done: case package readiness inspection now validates existing `annotation_task_path` and `annotation_import_path` artifacts after the OCR/Vision chain, surfacing redacted task evidence-ref errors, OCR/text leaks, or annotation imports that cannot rebuild a source-aligned final gold package as `fix_artifact_validation` before downstream analysis, monthly, delivery, or dataset commands.
- Done: case package readiness inspection now validates existing `gold_label_package_path` and `gold_label_review_report_path` artifacts, requiring final gold to match the same VisionEvidencePacket and regenerated annotation import package, and requiring the review report to match that package and be `ready_for_99_evaluation` before downstream commands continue.
- Done: case package readiness inspection now validates existing `analysis_path`, `result_path`, `monthly_report_input_path`, `monthly_report_path`, `delivery_bundle_path`, and `teacher_review_packet_path` artifacts, surfacing invalid analysis/result, monthly comparison, delivery, or teacher-review handoff artifacts as `fix_artifact_validation` before asset manifest or dataset commands.
- Done: case package readiness inspection now validates an existing `external_vision_input_path` as `ExternalVisionAdapterInput`; malformed real Provider outputs such as missing provider metadata, empty pages/questions, duplicate IDs, orphan page/region refs, invalid confidence, or cross-question attribution return `fix_artifact_validation` before `normalize_external_vision` or downstream labeling can continue.
- Done: external OCR/Vision adapter input and VisionEvidencePacket validation now reject evidence whose `region_id` belongs to a different `page_id`, preventing cross-page visual-region attribution before question segmentation, human gold, or text reasoning.
- Done: evaluation asset manifests now support `external_vision_input_path`; `validate:k12-eval-assets` validates the external Provider adapter input against the same VisionEvidencePacket and blocks 99% asset readiness when human-labeled cases lack replayable Provider input artifacts.
- Done: `generate:k12-eval-artifacts` preserves existing `external_vision_input_path` when writing expanded manifests, and `audit:k12-eval-claim` maps missing external Provider input blockers to a dedicated `external_provider_input` next-sample target instead of confusing them with Provider trial reports.
- Done: real-sample case package generation now writes helper-only `provider/external-vision-input.template.json`, giving real OCR/Layout/Vision Provider outputs a privacy-safe mapping guide into `provider/external-vision-input.json` without entering the asset manifest or 99% claim evidence.
- Done: real-sample case package generation and `generate:k12-eval-assets-manifest` now preserve `monthly_report_input_path`, so the teacher-confirmed monthly input artifact is visible from scaffold through asset preflight instead of being dropped from manifest cases.
- Done: deterministic material classification now ignores textbook publisher/version context when scoring regional exam signals, so phrases like `北京师范大学出版社` or `牛津上海版` do not become false Beijing/Shanghai region evidence while explicit region evidence is still preserved.
- Done: teacher delivery bundle validation now requires monthly summary, month-over-month comparison, and first-priority action to match either the validated user-facing result or the attached monthly report artifact, preventing UI handoff packages from inventing stronger longitudinal trends.
- Done: monthly report generation now derives previous-month comparison evidence counts, report-used status, and availability from replayable previous-month source ids, so stale or tampered previous report `readiness.source_count` values cannot produce invalid longitudinal evidence metadata.
- Done: monthly report generation now only reuses an optional `previous_report` when its student name, previous-month label, and replayable timeline source ids match the current input; file input validation rejects mismatched or duplicate-source previous reports before generation.
- Done: `generate:k12-monthly-report` input validation now treats `previous_report` as a full monthly artifact: `readiness.source_count`, `comparison_evidence.current_month_source_count`, replayable `evidence_timeline`, missing/available previous-evidence state, and parent-facing safety text must be self-consistent before it can serve as the baseline for current-month comparison.
- Done: `generate:k12-eval-artifacts` now reuses an existing `monthly_report_input_path` when generating monthly report output, instead of overwriting teacher-prepared monthly input or requiring new teacher-confirmation metadata.
- Done: asset preflight and case package readiness now validate `monthly_report_path` against the same `monthly_report_input_path`, requiring current-month `evidence_timeline` source ids and previous-month `comparison_evidence.previous_month_source_ids` to match the replayable input sources.
- Done: evaluation asset preflight now enforces same-question scope for side-input answer/rubric evidence refs in both human gold evidence basis and per-question analysis evidenceRefs, preventing q001 conclusions from being supported by q002 external answer keys.
- Done: dataset-level 99% claim gating now requires claim-ready `asset_preflight` metadata to belong to the current dataset: matching manifest/dataset id, recorded check time, and empty blockers when ready. `generate:k12-eval-dataset` now keeps this identity aligned even when a custom dataset id is supplied, preserving the original manifest id as `source_manifest_id`.
- Done: `generate:k12-eval-artifacts` can now generate upstream OCR/Vision and human-gold preparation artifacts before `analysis_path` exists; analysis remains required only for result/monthly/delivery steps that actually need model output.
- Done: live runner and asset preflight now reject process-based mistake diagnosis such as knowledge gaps, process omissions, checking habits, carelessness, concept confusion, or method errors unless the diagnosis cites same-question `student_process` or `student_note` evidence.
- Done: user-facing result generation now keeps parent feedback non-copyable and marks it `needs_teacher_review` whenever any question, risk flag, or analysis-level blocker still requires teacher review, so review-required evidence cannot become a directly copyable WeChat draft.
- Done: profile update suggestions now require same-packet definitive question evidence before remaining `draft`; suggestions citing teacher-review questions or `side_input.*` answer/rubric refs are rejected by runner, asset preflight, and case-package readiness.
- Done: runner now has direct regression coverage for `notes_only` and `teacher_mark_only` materials so they degrade before model calls, preserve missing-context hints, and cannot produce profile suggestions, confirmed monthly sources, or sendable parent feedback.
- Done: real-sample case package readiness now honors explicitly declared answer/rubric side-input paths and validates analysis artifacts with the same question evidence readiness, same-question side-input scope, process-mistake, and profile-update gates used by runner and asset preflight.
- Done: case package readiness now validates declared answer/rubric side-input files themselves: every entry must include `question_id` or `questionId` and the id must exist in the same VisionEvidencePacket before downstream labeling or reasoning can continue.
- Done: human gold package validation and annotation-import conversion now accept a same-question side-input whitelist, so `question_evidence_basis` cannot use another question's `side_input.*` answer/rubric ref in case-package readiness.
- Done: gold label review report generation, validation, asset preflight, artifact bundle generation, and case-package commands now reuse the same answer/rubric side-input whitelist, so stale ready reports generated without the declared `question_id -> side_input` map are rejected.
- Done: standalone `validate:k12-gold-labels` now also accepts `XUEMAI_ANSWER_KEYS` / `XUEMAI_RUBRICS` with `XUEMAI_VISION_PACKET`, so direct final-gold validation cannot silently ignore declared side-input question maps.
- Done: human gold package validation now checks `question_evidence_basis` slot evidence types against the source VisionEvidencePacket, so answer keys cannot be used as student traces and student answers cannot be used as answer/rubric or teacher-correction evidence.
- Done: human gold package validation now requires every `adjudicated_gold` definitive question to be supported by reviewer `question_evidence_basis` student-trace plus answer/rubric or teacher-correction evidence, preventing adjudication from inventing unsupported hard judgements.
- Done: open-source scan now explicitly separates OCR/Layout/Formula Provider candidates, annotation tools, and public K12 benchmarks; `validate:k12-eval-assets` now reports `publicBenchmarkSources` and blocks `human_labeled` 99% readiness when cases are marked as K12Vista/CMMaTH/CMM-Math/E-EVAL/EduEval/K12-Bench/K12-KGraph/OCRBench-style public benchmark or source dataset instead of real anonymized student material.
- Done: `audit:k12-eval-claim` now maps public benchmark/source-dataset asset-preflight blockers to a dedicated `replace_public_benchmark_source` artifact focus, so next-sample targets explicitly ask for real anonymized student materials instead of treating public benchmarks as claimable human-gold cases.
- Done: mainland region/curriculum claim coverage now reports and gates mainland regional groups, curriculum-version families, and national/new-gaokao exam-scope signals in dataset eval, asset preflight, and claim audit. Raw `regionsOrCurricula` count alone is no longer enough for the strict 99% claim policy.
- Done: `compare:k12-model-regression` now requires identical human-gold case content for matching baseline/candidate case IDs, preventing model/provider/prompt comparisons from silently changing the gold labels they are measured against.
- Done: `compare:k12-vision-provider-regression` now fails when a same-material candidate Provider keeps the same question IDs but reorders them, preventing OCR/Vision Provider switches from silently breaking per-question evidence alignment.
- Done: `compare:k12-vision-provider-regression` now also fails when a same-question candidate changes `page_id` or `question_number`, preventing Provider switches from keeping the same IDs while moving questions to a different page or visible number.
- Done: `compare:k12-vision-provider-regression` now includes page, total evidence, student-trace evidence, and answer-basis evidence counts in regression metrics, blocking Provider switches that silently lose the evidence needed for hard judgement and human-gold labeling.
- Done: `compare:k12-vision-provider-regression` now compares per-question `evidence_type_counts` aggregates, so summary-level evidence counts cannot hide one question losing student-trace or answer-basis evidence.
- Done: dataset eval results now retain the active 99% claim policy and both `eval:k12-material` reports and `audit:k12-eval-claim` reports print a shared `claimPolicy.requirements` line, making custom local policies visibly distinct from the strict human-labeled 99% gate.
- Done: `validate:k12-eval-assets` reports now print the same strict `claimPolicy.requirements` line before `claimable99AssetReady`, so asset preflight reviewers can see the human-labeled 99% gate before dataset generation or claim audit.
- Done: `inspect:k12-eval-case-package` readiness reports now print the same strict `claimPolicy.requirements` line while keeping `claimable99.fromCasePackage=no`, so a real-sample scaffold or half-complete package cannot be mistaken for 99% evidence.
- Done: annotation import conversion now fails whenever the generated final gold package fails source-aware validation, so cross-question evidence refs, wrong evidence basis slot types, or unsupported adjudicated hard judgements cannot be returned as a successful human-gold package conversion.
- Done: annotation import `allowIncomplete` draft generation now writes missing-adjudication packages only as non-claimable debug artifacts, keeping `claimable99Correctness=false` even when draft validation passes with `requireAdjudication=false`.
- Done: `validate:k12-eval-assets` now requires annotation import artifacts to reproduce the full final gold package provenance, including labels, reviewer metadata, question evidence basis, `adjudicated_gold`, and adjudicator metadata, not only adjudicated gold.
- Done: case package readiness now reuses the same full human-gold provenance comparison and reports specific final-gold drift fields, so reviewer label or adjudicator metadata changes cannot hide behind a generic annotation-import mismatch.
- Done: redacted annotation task validation now recomputes packet-derived task questions from the same VisionEvidencePacket, rejecting hand-edited segmentation status, review flags, definitive suggestions, issues, regions, evidence summaries, or evidence-basis suggestions before asset preflight can treat the task as usable human-gold input.
- Done: annotation import skeleton and Label Studio task payloads inside redacted annotation tasks are now validated against the same packet-derived task, so evidence refs, notes, crop refs, issues, and status hints cannot drift before human labeling or asset preflight.
- Done: evaluation asset preflight now rebuilds adapter-derived VisionEvidencePacket core fields from `external_vision_input_path` and rejects pages/questions/regions/evidences/gates or pipeline-stage drift before real Provider input can be treated as replayable claim evidence.
- Done: case package readiness now reuses the external Provider input -> VisionEvidencePacket rebuild gate, so an existing provider input and packet with drifted pages/questions/regions/evidences/gates or pipeline core fields returns `fix_artifact_validation` before downstream commands.
- Done: evaluation asset preflight now reports Provider role coverage and blocks `claimable99AssetReady` when real OCR/Vision cases lack OCR/document-parser, layout, or formula candidate coverage, so open-source candidate scans cannot become a shallow link checklist before 99% claims.
- Done: `audit:k12-eval-claim` now maps Provider role coverage blockers to `complete_provider_role_coverage:provider_role_coverage`, so missing OCR/document-parser, layout, or formula candidate coverage becomes an actionable next artifact target.
- Done: real-sample case package generation now surfaces Provider role coverage in the helper template and 99% checklist, so collectors see OCR/document-parser, layout, and formula candidate requirements before asset preflight.
- Done: case package readiness now keeps the Provider role coverage checklist missing until the existing VisionEvidencePacket trace actually includes OCR/document-parser, layout, and formula candidates.
- Done: case package readiness reports checklist present/missing counts and returns `complete_checklist_evidence` when a valid VisionEvidencePacket still lacks OCR/document-parser, layout, or formula candidate roles.
- Done: case package readiness now reports `providerTrialReadiness` and returns `fix_provider_trial_readiness` when a structurally valid Provider trial report is still `blocked`, stopping downstream human labeling until OCR/Vision output is fixed.
- Done: open-source Provider scan now adds dots.mocr as a `benchmark_only` document-parser candidate in the default VisionEvidencePacket provider trace, with tests and docs preserving the boundary that it cannot bypass K12 evidence gates or human-gold evaluation.
- Done: case package readiness now validates `evaluation-assets.cases.json` against the same `case-package.json` dataset metadata and unique `evaluation_asset_case`, so tampered asset-manifest entry paths stop as `fix_artifact_validation` before downstream Provider, human-gold, monthly, delivery, or dataset commands.
- Done: `generate:k12-eval-assets-manifest` now rejects object-form cases-file dataset metadata overrides that differ from the file's declared `dataset_id`, `dataset_kind`, or `description`, preserving case-package provenance when building the formal asset manifest.
- Done: gold label review report validation now replays disagreement field/message/hash details from the same final gold package, so hand-edited disagreement rows are rejected by both standalone review validation and `validate:k12-eval-assets`.
- Done: legacy/mock user-facing result generation now follows the same monthly-comparison evidence rule as the formal result path: without replayable previous-month source evidence, UI previews show missing-evidence copy instead of explicit month-over-month progress/regression.
- Done: source-packet human gold validation now rejects `side_input.*` answer/rubric refs unless the caller provides a question-mapped side-input whitelist, so annotation imports and standalone gold packages cannot treat undeclared external answers as 99% evidence.
- Done: case package readiness now recursively flags nested raw/source-material directories and raw image/PDF-like files, so real-sample packages cannot continue to Provider, human-gold, monthly, delivery, or asset-preflight steps while carrying original student materials.
- Done: case package readiness now rejects internal `artifact_paths` / `evaluation_asset_case` drift, so package command paths and formal asset-manifest entry paths cannot silently point to different OCR/Vision, human-gold, monthly, delivery, or teacher-review artifacts.
- Done: case package readiness now rejects generated `command_sequence` drift, so the next recommended Provider, human-gold, monthly, delivery, teacher-review, or asset-manifest command must stay derived from the same artifact paths.
- Done: case package readiness now has a positive full-chain replay regression from external Provider input through VisionEvidencePacket, Provider trial, segmentation QA, redacted annotation task, annotation import, final gold, gold review, analysis/result, monthly input/output, delivery bundle, and teacher review packet, ending at the formal evaluation asset manifest step while keeping 99% unclaimable.
- Done: case package readiness now has a follow-on replay regression after `evaluation-assets.json` generation: the asset-manifest command becomes complete, `asset_preflight` is inspected, `fix_asset_preflight` blockers such as mock/synthetic sources and insufficient representative coverage surface, and `claimable99_from_case_package` remains false.
- Done: generated eval datasets now have an integration regression proving the same asset-preflight blockers flow into `audit:k12-eval-claim`, where they become artifact-focused next sample targets such as replacing mock Vision sources, completing external Provider inputs, Provider trial reports, and monthly comparison evidence.
- Done: `npm run generate:k12-eval-sample-plan` now converts `audit:k12-eval-claim` next sample targets into a privacy-safe real-sample collection plan with case package command templates, suggested case counts, artifact-focus notes, and an explicit boundary that the plan is not 99% correctness evidence.
- Done: `npm run generate:k12-eval-sample-packages` now creates privacy-safe case package scaffolds from a sample plan, with optional staged batch caps and standard OCR/Vision, human-gold, monthly, delivery, and teacher-review artifact paths while keeping raw student materials out.
- Pending: enable Auth/RLS-aware access policies before multi-teacher production use.
- Pending: execute `supabase/migrations/202606120001_parent_feedback_confirm_idempotency.sql` in Supabase before using the real confirm API in a shared environment.
- Pending: review and manually execute `supabase/migrations/202606120002_skillcard_persistence_contract.sql` before syncing SkillCard state to Supabase.
- Pending: review and manually execute `supabase/migrations/202606120003_skill_shell_rls_policies.sql` only after Auth teacher_id mapping is confirmed.
- Pending: run `supabase/checks/202606130001_skill_shell_readiness_check.sql` in Supabase SQL Editor after the Skill Shell migrations.
- Pending: only set `SKILL_PERSISTENCE_WRITE_ENABLED=true` in a confirmed test/prototype environment after Skill Shell migrations and RLS are verified.
- Done: LessonLedger 学习报告已从“生成即保存”拆成“生成草稿 / 确认保存”两步；草稿默认不对家长可见，确认保存后才进入家长端报告列表，并保留家长可见摘要。
- Done: 学生档案的成绩和薄弱点已补齐编辑入口；修改考试名/日期或薄弱点名称时会通过后端 update 动作替换旧记录，而不是生成重复记录。
- Done: 课时财务流水新增收款金额字段；手工充值必须记录现金金额，工作台和财务页“本月实收”改为从真实现金流水汇总，不再用已完成课程价格近似。
- Done: 财务页的“全部流水 / 充值 / 扣课 / 撤销 / 待结算”已接真实筛选；账务待处理改为从低余额、未收款扣课流水和撤销流水动态生成。
- Done: 家长端首页、课程页和通知只展示已保存且家长可见的学习报告；课程记录里的查看反馈/沟通会按对应课程匹配反馈，没有已发送反馈时禁用入口。

## Next Steps

1. Keep the real Skill persistence write path disabled by default.
2. Prepare a controlled Supabase test environment before enabling `SKILL_PERSISTENCE_WRITE_ENABLED=true`.
3. Generate a real-sample case package with `npm run generate:k12-eval-case-package`, use `npm run inspect:k12-eval-case-package` immediately to confirm `evaluation-assets.cases.json` still matches `case-package.json.evaluation_asset_case`, use its helper-only `provider/external-vision-input.template.json` to map real PaddleOCR/PaddleX sample outputs into `provider/external-vision-input.json`, use readiness after each added artifact to confirm the next sequential command and blockers, re-run readiness until `external_vision_input_path` validation passes, run `npm run generate:k12-vision-packet`, fix any VisionEvidencePacket page/question/region/evidence/gate/trace cross-reference errors, run `npm run generate:k12-provider-trial-report` and `npm run generate:k12-question-segmentation-review`, then create redacted annotation tasks with `npm run generate:k12-gold-label-annotation-task`.
4. Use Label Studio or CVAT as upstream annotation tools if helpful, normalize exports into `student_learning_material_gold_label_annotation_import.v0.1`, save it at each case package's `human-review/annotation-import.json`, run `npm run generate:k12-gold-label-package` with the same `XUEMAI_QUESTION_SEGMENTATION_REVIEW` plus any declared `XUEMAI_ANSWER_KEYS` / `XUEMAI_RUBRICS`, then validate each final package with `npm run validate:k12-gold-labels` using the same `XUEMAI_VISION_PACKET` and side-input paths, and generate `npm run generate:k12-gold-label-review-report` with those same inputs.
5. Prepare each case package's `human-review/monthly-report-input.json` from teacher-confirmed current-month material snapshots and replayable previous-month sources or explicit no-baseline evidence, then run the package's `monthly_report` command before delivery bundle generation.
6. Build an evaluation asset manifest for those samples with `npm run generate:k12-eval-assets-manifest`; when using object-form `evaluation-assets.cases.json` from a case package, do not override `dataset_id`, `dataset_kind`, or `description` unless the value is identical. Generate analysis artifacts, expand the manifest with `npm run generate:k12-eval-artifacts` so external Provider input/provider trial report/question-segmentation review/annotation task/annotation import/gold-label-review/result/monthly/delivery bundle/teacher review packet artifacts are present, then run `npm run validate:k12-eval-assets`; review external Provider input identity errors, provider/readiness warnings, Provider candidate trace/source URL/license coverage, annotation-import-to-gold alignment, gold-label-review readiness, result/delivery/review-packet/monthly-comparison quality errors, Vision source-kind coverage, mock/synthetic source blockers, mainland regional group / curriculum-version family / national-new-gaokao coverage blockers, and claim-policy warnings before treating the dataset as representative. Only after `claimable99AssetReady=true`, run `npm run generate:k12-eval-dataset` so the dataset carries the same `asset_preflight` result and per-case manifest/gold-package/analysis provenance, run `npm run audit:k12-eval-claim` to find remaining coverage gaps, use `npm run generate:k12-eval-sample-plan` and then `npm run generate:k12-eval-sample-packages` to turn remaining gaps into the next real-sample collection batch if blockers remain, then run `npm run eval:k12-material` for claimable correctness only when the audit and preflight evidence are ready.
7. Add province/city-specific exam and textbook adapters only after real regional samples or user-provided institution rules are available.
8. Connect the confirmed monthly snapshot copy helper, monthly-report artifact generator, and unified monthly source pool to real repository adapters after Auth/RLS and data tables are confirmed.
9. Run the same human-labeled dataset when switching `AI_PROVIDER`, `AI_BASE_URL`, `AI_MODEL`, prompt, or schema.
10. For each OCR/Vision Provider switch, keep same-material baseline and candidate Provider trial reports and run `npm run compare:k12-vision-provider-regression` before downstream reasoning or gold labeling; for formula-heavy math materials, include formula benchmark candidates such as LaTeX-OCR/Pix2Text/UniMERNet in the provider trace, but still require question segmentation, same-question answer basis, and human gold validation before any definitive correctness claim. For each reasoning-model/provider/prompt/schema switch, keep both baseline and candidate dataset outputs and run `npm run compare:k12-model-regression` before treating the candidate as accepted; the comparison must keep user-facing result, teacher-confirmed monthly report input/output, delivery bundle, and teacher review packet all valid.
11. Wire `student_learning_material_delivery_bundle.v0.1` into the real SkillCard/analysis detail UI after backend persistence boundaries are confirmed; keep the delivery bundle validator in the API path so UI cannot receive incomplete teacher-facing results or missing structured classification.
12. Wire `student_learning_material_teacher_review_packet.v0.1` into the SkillCard shell as the UI-facing artifact; it may be produced from an existing delivery bundle or from result/analysis/VisionEvidencePacket inputs through the same command, but UI actions must remain reviewable and cannot auto-send WeChat or auto-write archive/profile fields.

## Verification Focus

- Codex reads project rules from `AGENTS.md`.
- Future work uses phase-level summaries instead of daily operation logs.
- Architecture remains reusable for future agents beyond Parent Feedback Agent.
- Parent Feedback Agent must keep AI drafts separate from teacher-confirmed records.
- Confirmed save flow must not auto-send WeChat or auto-update `students` profile fields.
- Server-side Supabase writes must keep using the server-only service-role client until Auth/RLS policies are implemented.
- Current frontstage product work should prefer `src/skills/` over expanding `src/agents/`; `src/agents/` remains backend capability reserve.

## 2026-08-06：完整产品功能结构

- Done：新增 `docs/产品功能结构.md`，形成个人版、机构版统一的完整产品功能总地图。
- Done：功能按 5 个一级入口、20 个模块和 P0/P1/P2 三阶段组织，覆盖招生衔接、教学记录、材料分析、家长沟通、长期档案、服务跟进、续费准备、机构协作与数据衔接。
- Done：补齐服务周期、完整服务时间线、批量管理、数据接入与历史迁移，同时保持不做复杂排课、完整财务、人事工资、自动营销和学生任务中心的范围边界。
- Done：新增本体模型，将学生、关系、事件、证据、结论产物和责任承诺定义为稳定产品对象；总览与待处理合并为工作台，批量管理改为操作模式。
- Next：基于五个主页面输出信息架构和首版页面线框，并对 P0/P1 做真实用户优先级验证。
