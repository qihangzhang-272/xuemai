# 学脉独立项目验收报告

验收日期：2026-09-04（本机时区 America/New_York；证据 JSON 使用 UTC）。

本轮完成会议收敛后的教学服务闭环，并通过本机生产构建、真实模型、文档解析和 MDT 只读接入验收。这里的验收使用合成教学材料，不等同于真实教师课堂 UAT。

## 独立交付

项目位于 `C:/Users/Administrator/Desktop/AI/codex/xuemai`，有独立 Git 仓库和 `codex/xuemai-independent` 分支，服务监听 `127.0.0.1:3016`。用户提供的源码作为初始提交保留，本轮实现与验收分别记录。

账号、会话、学生副本、课堂记录、家长反馈、学生档案和文件均保存在新项目的 `.xuemai-data`。生产启动脚本固定使用项目根目录的数据路径；重启后已验证原有记录与登录状态仍可读取。应用不依赖其他项目目录，不需要原项目的本机进程运行。

原项目 `multi-dimensions-teacher` 的 HEAD 仍为 `7a9c201e9ae77763296353993d4aff6a04ea9217`，没有跟踪文件改动；原有未跟踪的 `.workbuddy/` 和 BP 文档保留。本轮未执行原项目迁移、业务写入、部署或服务重启。

## 复用来源与实现范围

| 来源 | 本轮使用方式 |
| --- | --- |
| 用户提供的学脉 Web / Mobile 源码 | 保留绿色品牌、Logo、会话工作台结构和基础表单组件；当前入口使用真实服务与持久化数据 |
| MDT MinerU 解析器、解析任务错误类型 | 直接复制至 `lib/mdt`，解析工件改存学脉目录，保留任务 ID 以支持重试 |
| MDT 正文质量校验与密码工具 | 复制 UTF-8 材料校验和 bcrypt 校验，维持 72 字节上限 |
| MDT 服务配置与 API | 复用千问及 MinerU 配置；教师认证和班级 / 学生统计通过服务器端适配器读取 |
| 会议白板与产品需求 | 以会议中的备课、课堂 / 材料分析、可复制的家长反馈、本机交付为本轮范围；PRD 补充确认、版本和档案规则 |

已提供独立注册 / 登录、学生与班级管理、班级导入、AI 备课、课堂记录、材料分析、原稿对照、老师修订、反馈生成 / 编辑 / 复制、手工标记已发、单独确认入档、学生月报、班级逐学生草稿、老师偏好、JSON 导出与本机备份。未完成输入和编辑草稿在当前浏览器保存。

历史原型页面/API 由当前入口禁用，历史 Supabase、微信协作、出题和批改源码不是本期可用功能。新工作流不以虚构输出作为模型失败回退。

## 实际验证结果

| 检查 | 结果与依据 |
| --- | --- |
| 类型、Lint 与生产构建 | 通过；Next.js 15.5.22，生产 BUILD_ID `zcYNKLkOgTnm8ujxBYOUu`；正式首页共享加载约 127 kB |
| 单元检查 | `npm test`：2 个文件、40 项通过。包含账号数据隔离、版本冲突、事务回滚、生成失败保留、长时间中断恢复、发送 / 入档分离、月报来源、日期、密码和解析安全 |
| 正式版 HTTP 闭环 | `npm run test:smoke`：31 次接口检查，以及旧 API 禁用、跨站写入拒绝等断言通过；真实调用模型完成课堂记录、反馈、月报、备课和 TXT 材料分析 |
| 多类型文件 | `npm run test:materials`：PDF、作答图片、空白试卷图片、DOCX、PPTX 全部通过真实上传与解析 / 生成；加上前项 TXT，共 6 类测试素材 |
| MDT 真实接入 | 原教师账号登录成功；读取 3 个班级，选择非空班级读取和导入 1 位学生；重复导入不增加人数；没有自动虚构本地学情记录；远端班级列表前后一致 |
| 浏览器完整操作 | 实际完成学生创建、课堂整理、修订、反馈生成及编辑、剪贴板复制、手工标记已发、独立入档和月报；班级流程排除一位学生后，只为选中的学生生成个人记录 |
| 正式版响应式布局 | 390 × 844 视口下，工作台 / 学生 / 班级 / 我的 / 会话均无横向溢出；桌面默认 1280 × 720 预览正常；正式版浏览器错误 / 警告日志为空 |
| 草稿保留 | 在正式版填写课堂草稿、切换学生名单、返回会话，输入仍保留；未提交为业务记录 |
| 备份可读性 | `npm run backup` 成功；独立打开备份数据库，`PRAGMA quick_check` 为 `ok`；备份时 3 个账号、7 个会话、26 个业务实体数量一致，15 份材料 / 解析文件 SHA-256 一致 |

正式版材料测试的单次观测耗时：PDF 15.3 秒、作答图片 2.2 秒、空白试卷 1.3 秒、DOCX 16.2 秒、PPTX 17.4 秒。这里只是本轮小型合成材料的实测，不是性能承诺。

空白试卷被标为“证据不足”，直接入档返回 400；备课不能生成个体反馈或进入学生档案；已发送或已入档正文不可覆盖；月报只使用所选月份的已入档课堂 / 材料分析快照。复制操作不更改发送状态，也没有实际向任何家长发送消息。

## 证据与使用入口

- [正式版 HTTP 检查](qa/http-smoke.json)
- [材料解析检查](qa/material-smoke.json)
- [MDT 连接检查](qa/mdt-smoke.json)
- [正式版手机布局与控制台](qa/production-browser.json)
- [备份校验](qa/backup-verification.json)
- [桌面工作台](qa/desktop-overview.png)、[桌面会话](qa/desktop-chat.png)、[手机会话](qa/mobile-chat.png)

启动方式与环境变量见根目录 `README.md`；Windows 可运行 `start.ps1`。本机体验账号见未纳入 Git 的 `.local-access.txt`，已配好本机服务配置。源码交付包通过 `git archive` 生成，不含密钥、数据库、上传原件、浏览器草稿或 `node_modules`。

## 尚未验证或不在本期范围

本轮没有公网部署、真实教师课堂 UAT、持续负载测试或完整 PRD 的机构协作 / 小程序 / 收费排课 / 微信消息直连 / AI 出题批改。手机验收为浏览器视口模拟，未声称做过实体手机兼容性测试。当前 SQLite 和任务执行方式适用于单机单实例，AI 与文档解析依赖外部服务配置及可用性。

真实试用时，老师仍需检查生成结果，尤其是图像识别、能力解释和建议是否符合材料。所有测试学生、课堂材料和界面截图中的观察均为合成验收数据。
