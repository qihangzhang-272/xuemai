# 学脉独立演示动线精简

用户要求：继续使用提供的原前端，修复正文裁切，降低初次使用的理解成本；作为独立演示项目，不出现或连接旧教学项目。

参考 Impeccable 官方技能的 distill、clarify、onboard 和 Operate 指南。保留原版的视觉语言，主要调整操作层级。官方当前技能引用的 context.mjs 未在仓库发布，本轮依据实际代码、截图和用户指令确定产品上下文。GPT Taste 偏重展示网站动效，不用于本次教学工作台。

1. 移除旧项目账号登录、连接表单与远程读取接口，保留已复制的通用本地能力；不删除已有账号和教学数据。验证：接口拒绝旧登录模式，旧连接路由不可访问，源码不再读取旧服务配置。
2. 将主要流程收敛为选择学生、记录课堂、检查并入档、生成家长反馈、复制发送。输入默认整理课堂记录，补充工具逐步展开；报告有明确入口；没有学生时引导建立第一位学生。验证：浏览器无需猜测按钮用途即可完成一次流程。
3. 统一结果卡片与详情的操作顺序，每处最多一个主要动作；正文自然排版，不用固定高度裁切；详情单独阅读。简化注册、建档、设置。验证：长正文、公式、归档状态和手机窄屏的实际呈现。
4. 运行类型、检查、项目测试和生产构建；桌面与手机集中复测，记录证据和限制，更新交付包并启动最终版本。
