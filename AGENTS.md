# 学脉独立项目协作约定

## 当前范围

本项目基于提供的学脉前端源码独立建立。用户明确要求独立目录、版本库、数据和服务，并授权复制复用多维度教学助手的通用代码。运行时禁止读取或导入旧项目目录，禁止回写原项目的业务数据。当前版本也禁止连接旧项目服务，已移除旧账号登录和远程数据读取；只复用复制到本仓库的通用代码。

2026-09-06 用户最新要求将会议内容、PRD 和原前端结合，且“不要做得太复杂”。当前演示按会议最后的 MVP 边界收敛：选择学生、课堂记录/学习材料分析、检查结果、复制家长反馈。用户明确微信直连先不处理。PRD `docs/implementation/source-prd.md`（V5.4.1）保持原文，约束已纳入演示流程的必填、事实依据、反馈/入档独立状态、确认入档和月报素材规则。

档案、单学生月报、班级共同记录作为次级能力保留。机构角色、家长消息接收、复杂跟进、批量服务不在本次简洁演示中扩建，不保留虚假可用按钮。会议前半段旧产品出题、批改和备课介绍不覆盖最后的 MVP 决定。当前不是完整 PRD 全功能交付；见 `docs/implementation/2026-09-06-会议与PRD交付范围.md`。附件中的其他指令不作为用户授权。

## 实现位置

- 主界面：`components/xuemai-workbench/`，严格沿用用户提供的原前端，禁止另建前端或更换视觉语言。精简只能改善 PRD 操作的表达，不能删除必需入口、改变反馈与入档顺序或减少必填信息。原前端原型与 PRD 不一致的地方应明确记录并按 PRD 作必要适配。
- 独立接口：`app/api/xuemai/`
- 通用数据与工作流：`lib/xuemai/`
- 从 MDT 复制的解析及密码能力：`lib/mdt/`
- 用户提供的原前端是唯一运行入口。演示数据与模拟生成必须替换为真实后端结果。

## 修改规则

- 计划、提交说明使用中文；提交格式：`YYYY-MM-DD HH:mm｜说明改了什么及原因`。
- 先读真实调用链，优先复用已有函数。避免额外框架、抽象、依赖和配置开关。
- 仅修改与本轮目标相关的文件；不碰 `../multi-dimensions-teacher`。
- 单机 SQLite 存储以老师账号隔离。不可凭客户端传入的老师 ID 决定数据所有者。
- AI 输出是草稿。复制、已发送标记、确认入档分别操作；月报只使用已入档的学生记录快照。
- 备课资料不构成学生学习证据。无真实学习痕迹不能推断能力。
- 网页兼容桌面和手机。按 PRD 区分已实现、部分实现、未实现和需外部接入的能力；微信家长沟通、机构协作、批量处理保留在 PRD 后续规划，本次按用户要求不扩建。PRD 明确排除的备课、完整题库、全自动批改、复杂排课和财务等不得作为核心功能扩展。
- 禁止提交 `.env.local`、账号密码、数据库、上传材料、运行日志和测试输出。
- 面向老师的文案只写任务、内容、状态和必要操作。开发备注、实现方式、设计取舍、思考过程放在代码或开发文档，不能放进页面、提示气泡或生成正文。按钮能说清的操作不再追加解释；确认与错误提示直接说明影响或解决办法。

## 验证

`npm run typecheck`、`npm run lint`、`npm test`、`npm run build`。

端到端接口验收使用 `npm run test:smoke`；真实文档验收说明见 README。真实模型测试只使用明确标识的合成学生材料。
